# Проверка Career Quest по заданию

## Текущий маршрут развития

Маршрут использует активности из каталога для активной цели сотрудника. После
завершения шага применяются его эффекты, сохраняется история и пересчитываются
рекомендации. Когда требования цели выполнены, интерфейс переключается на следующую
цель. Предварительный следующий этап появляется при точном покрытии от 80%.
Фактический грейд и исходная карьерная цель не меняются автоматически.

Шаблонные практические задания удалены из маршрута. Прежние отметки практики
не отображаются и не учитываются в прогрессе; её endpoint завершения возвращает 410.
Если каталог не содержит полезного доступного шага, показывается необходимость
согласовать развитие с HR и дополнить каталог. Актуальный сценарий ручной проверки
описан в [jury-demo.md](jury-demo.md), контракт — в [api-contract.md](api-contract.md).

На синтетическом `demo.json` проверен расчёт маршрута Алии: Middle 38% → после
Python 63% → после SQL 88% и предварительный Senior → после «Демо для команды»
активный Senior 62% → после «Проекта с наставником» Senior 85% и сообщение об
отсутствии доступных шагов.

Актуальная автоматическая проверка: **47 тестов прошли** (`node --test`).
В отдельном демо-хранилище также пройден сценарий в Chrome: выбор и отмена шага,
завершение активностей, предварительный Senior при 88%, переключение Middle →
Senior без повышения фактического грейда, новые рекомендации и сообщение при
исчерпании каталога. Проверены история, сохранение выбранного шага после обновления
страницы, восстановление прогресса после перезапуска сервера и открытие HR-экрана.
На ширине 390 px горизонтального переполнения нет; ошибок JavaScript не обнаружено.

После объединения с коммитом `6373980` повторно прошли 47 тестов и браузерный
сценарий. Дополнительно проверены надписи «Добавить в план» / «В вашем плане»,
блокировка кнопки уже выбранного шага и счётчик завершённых шагов (0 → 1 → 3 → 4),
включая его сохранение после перезапуска. Счётчик требований подписан как число
навыков на нужном уровне и относится к активной цели.

Результаты ниже относятся к предыдущим версиям и не подтверждают новую реализацию.
В частности, самостоятельная шаблонная практика больше не является функцией
приложения. Сведения о старых прогонах сохранены как история проверок.

## Архив: объединение доработок команды

Изменения `296acdc` и `c61273b` объединены с самостоятельным выбором задач
и локальной практикой. После объединения **42 теста прошли**. Дополнительно
проверены выбор задачи для следующей цели после 100% текущей, сохранение такого
выбора после перезапуска, варианты за пределами первых трёх рекомендаций,
отсутствие дублей курсов и сохранение обоснований текущей цели при пересечении.
Фильтр навыков включает дефициты дальнейших целей. Права доступа, история
практики и подтверждённые уровни навыков сохранены.

## Архив: продолжение развития после цели

После добавления автоматических целей поверх `f9e1153` выполнен `npm.cmd test`:
**36 тестов прошли**. Новые проверки покрывают точный порог 80% до округления,
полное выполнение, пропуск уже достигнутых следующих грейдов, смену целевой роли,
доступные цели по навыкам на последнем грейде и отсутствие подходящих активностей.
HTTP-сценарий проверяет обновление плана, сохранение прогресса после перезапуска
и запрет повторного начисления. Текущий грейд и career_goal не меняются.

В браузере на независимом демонаборе проверен путь: Практикум Python → SQL →
покрытие 87.5% и новая цель Senior → Демо для команды → 100% → Проект с наставником
для дальнейшей цели. Ширина 1280, 390 и 320 px; горизонтального переполнения и
ошибок JavaScript в этом сценарии нет. Выдача доступа остаётся исключительно
через HR; изменения паролей не включены в эту доработку. Внешних AI API нет.

## Архив: проверка базовой версии

Проверка выполнена 23 сентября 2026 года для исправлений и нового интерфейса
поверх базовой версии `d06363f`.

## Архив: что проверено

| Требование | Подтверждение |
| --- | --- |
| Профиль и траектория | Полный набор: 200 сотрудников, 60 навыков, 32 профиля ролей и грейдов, 40 мероприятий |
| История | Все 2 743 записи XLSX преобразованы в локальный CSV и загружены |
| Рекомендации | Проверены 355 рекомендаций по всем 200 профилям: до 3 на человека, доступность аудитории, prerequisites, отсутствие mandatory и повторно завершённых неповторяемых событий, положительный эффект для цели |
| Объяснимость | Расчёт использует цель/грейд, разрывы и критичность навыков, личную историю типа и формата; прогноз завершения явно обозначен как модельный |
| Прогресс | Через HTTP завершена активность, повторное завершение отклонено, прирост сохранён после перезапуска |
| Проверочные профили | Через HTTP добавлен отдельный синтетический профиль в схеме кита; стало 201, прогресс остальных сохранился |
| HR | Сводки дефицитов, отсутствия шагов и участия открываются в браузере |
| Права | Без входа — 401; чужой профиль и HR-операции для сотрудника — 403; импорт и смена кода отзывают действующие сессии |
| Интерфейс | В браузере проверены HR-вход, профиль, смена целевой роли, завершение и обновление прогресса; ошибок JavaScript в проверенном сценарии нет |
| Новая компоновка | Вкладки рекомендаций, навыков и истории; отдельные вкладки HR; список сотрудников по алфавиту, поиск и переход со страницы 1–12 на 13–24 |
| Адаптивность | Проверены широкий экран 1280 px и узкие экраны 390 и 320 px; таблицы имеют собственную область прокрутки |
| Быстродействие | Один холодный запрос /api/state на полном наборе занял около 75 мс в локальном HTTP-прогоне; это не нагрузочный тест и не измерение полного времени отрисовки |

Браузерные операции выполнялись в отдельном тестовом хранилище. Исходные файлы
кита не изменены. В браузере подтверждено изменение Public Speaking с 0 до 1
и покрытия требований с 61% до 64% на тестовой копии профиля.

## Архив: исправления

- career_goal влияет на требования, включая смену роли; без цели остаётся следующий грейд.
- Начатая активность не предлагается как новое участие.
- При импорте клуба EV_036 один день даёт прирост только один раз; исходные строки истории сохраняются.
- Шкала 0–5 проверяется также в упрощённом JSON-импорте.
- Невалидная карьерная цель отклоняется.
- Набор, на котором нельзя построить dashboard, отклоняется до сохранения.
- Отозванный код доступа и уведомление другого профиля не остаются на экране.
- Отсутствие цели не выдаётся как 100% готовности в API.
- Файлы секретов, локальные данные и черновики исключены из Git.
- Профиль, цель и прогресс собраны в одной карточке; рекомендации вынесены на первый экран маршрута, полные объяснения раскрываются внутри карточек.
- Списки сотрудников и навыков отсортированы по алфавиту. HR-таблица показывает по 12 сотрудников и поддерживает поиск; рейтинг рекомендаций остаётся по полезности, история — по дате.

Команда проверки: `npm test`, все 25 тестов прошли. Автоматические тесты используют независимые
вымышленные примеры; файлы кита не включены в тесты или репозиторий.

## Архив: самостоятельный выбор и практика

Дополнительно проверены личный выбор задачи вне первых трёх рекомендаций,
защита от обхода prerequisites, автоподбор практики при 0–1 рекомендации,
неповторяемость этапов, сохранение выбора и результатов после перезапуска,
разграничение прав и отклонение поддельного прогресса из импорта. Практика
не меняет подтверждённые навыки и официальную историю. При загрузке полного
тестового состояния расчёт по 201 профилю занял около 42 мс; это локальное
измерение, а не гарантия для другого оборудования.

## Ограничения и оставшиеся действия

- Модель локальная статистическая; LLM и внешние API не подключены. Качество на закрытых профилях жюри ещё не проверено.
- UI принимает CSV, не XLSX. Для этой рабочей копии CSV уже подготовлен в игнорируемой папке data/starter-kit.
- Кнопка завершения моделирует выполнение, а не реальную регистрацию на дату сессии или подтверждение наставником.
- Текущее API завершает новую рекомендованную активность; изменение существующей записи in_progress в completed отдельным действием не реализовано.
- В истории источника обнаружены 474 повторных завершения, для которых адаптер подавляет повторный прирост. Это не удаление исходной истории.
- Нужно проверить запуск на компьютере напарника и подготовить короткий сценарий защиты.
- Если до подключения data/source.json приложение уже сохраняло демо, сохранённое состояние имеет приоритет. Подключать полный набор нужно через импорт или отдельное хранилище, не затирая нужный прогресс.
- Исходный датасет остаётся в старой истории Git после прежнего удаления. Переписывание общей истории в этой работе не выполнялось.

Проверка подтверждает перечисленные сценарии, но не гарантирует прохождение
неизвестных проверок жюри или готовность к промышленному развёртыванию.
